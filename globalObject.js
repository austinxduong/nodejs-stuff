console.log(); //<-- we call this a "Global Object". we can access this anywhere, in any files

console.log("1. we have Global Object in node. We don\'t have window object")

console.log("2. what is a module?")

console.log("3. piece of code that contains functionality. functionality that can also be accessed, and easily pluggable. can be easily deployed. this is most objective description as I can put it... ")
// oh, every file is a module essentially

console.log(module)

// now lets create a 'module'
// see login.js